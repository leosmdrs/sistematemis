PyQt6>=6.6
PyMuPDF>=1.24
Pillow>=10.0
PyQt6-WebEngine>=6.6
faster-whisper>=1.2
sherpa-onnx>=1.13

# Reconhecimento óptico pelo motor do próprio Windows (Varredura e PDF
# Pesquisável). Custa 3,5 MB, contra ~50 MB do Tesseract.
winrt-runtime>=3.2
winrt-Windows.Media.Ocr>=3.2
winrt-Windows.Graphics.Imaging>=3.2
winrt-Windows.Storage.Streams>=3.2
winrt-Windows.Globalization>=3.2
winrt-Windows.Foundation>=3.2
winrt-Windows.Foundation.Collections>=3.2

# Captura do som que o Windows reproduz, para a Gravação de Tela e a
# Extração Registrada. O FFmpeg no Windows só enxerga dispositivos
# DirectShow, e som de saída não é dispositivo de entrada: numa estação
# comum não há retorno a gravar. Esta biblioteca conversa com a interface
# de áudio do Windows por ctypes — sem compilar nada e sem pedir
# instalação na estação.
soundcard>=0.4.4

# Análise de Planilha. O leitor em Rust abre cem mil linhas em 15 s,
# contra 133 s do leitor comum em Python — e ocupa 1,6 MB. O openpyxl
# entra só para gravar o resultado. Nada de pandas: somaria uns 35 MB e
# converteria tipo por conta própria, que é justamente o estrago que
# esta ferramenta existe para evitar.
python-calamine>=0.8
openpyxl>=3.1
